<%= form_with(model: organization_role, local: true) do |form| %>
  <% if organization_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_role.errors.count, "error") %> prohibited this organization_role from being saved:</h2>

      <ul>
        <% organization_role.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
