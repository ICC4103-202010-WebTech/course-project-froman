<div class="row ml-0 mt-3">
  <h2>Edit "<%=@event.name%>"</h2>
</div>
<div class="row ml-0 mt-2">
  <p>Please enter the required information to edit "<%=@event.name%>":</p>
</div>
<%= render partial: "form",
           locals: {
               event: @event,
               submit_btn_text: "Update"
           } %>
