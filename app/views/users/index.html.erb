<p id="notice"><%= notice %></p>

<h3>Users</h3>

<tbody>
<% @users.each_with_index do |user| %>
  <tr>
    <li><%= link_to user.username, user_path(user.id) %></li>
    <br>
  </tr>
<% end %>
</tbody>

<br>

<%= link_to 'Back', :back %>